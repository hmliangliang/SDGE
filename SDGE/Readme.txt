----------------------------------------------------------------------------------------------------------
SDGE_GPU.py is available with GPU for graph embedding if graph is not too large.
SDGE_CPU.py is available with CPU for graph embedding if CUDA is out of memory (graph is too large).
----------------------------------------------------------------------------------------------------------
Requirements:

pytorch >= 1.5.1
numpy >= 1.18.1
scipy >= 1.4.1
sklearn >= 0.22.1
networkx >= 2.4
tqdm >= 4.42.1
dgl >= 0.4.3.post2
matplotlib >= 3.1.3
---------------------------------------------------------------------------------------------------------